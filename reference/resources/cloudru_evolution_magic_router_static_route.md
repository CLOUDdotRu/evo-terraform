# cloudru_evolution_magic_router_static_route (Resource)



## Example Usage

```terraform
resource "cloudru_evolution_magic_router_static_route" "resource_static_route" {
  # Поле magic_router_id является неизменяемым. При изменении значения ресурс будет пересоздан.
  magic_router_id = "f47ac10b-58cc-0372-8567-0e02b2c3d479"
  # Поле subnet является неизменяемым. При изменении значения ресурс будет пересоздан.
  subnet = "192.168.0.0/24"
  # Поле description является неизменяемым. При изменении значения ресурс будет пересоздан.
  description = "Description"
  # Поле next_hop_vpc является неизменяемым. При изменении значения ресурс будет пересоздан.
  # Нужно заполнить одно из значений - next_hop_vpc, next_hop_magic_router
  next_hop_vpc = {
    # Поле az_name является неизменяемым. При изменении значения ресурс будет пересоздан.
    az_name = "ru.AZ-1"
    # Поле vpc_connection_id является неизменяемым. При изменении значения ресурс будет пересоздан.
    vpc_connection_id = "f47ac10b-58cc-0372-8567-0e02b2c3d479"
  }
  # Позволяет переопределить дефолтный таймаут провайдера для определенного метода. Если нужно указать бесконечный таймаут, то нужно указать например 0s, тогда таймаута не будет.
  timeouts {
    create = "60m"
    update = "30m"
    delete = "20m"
  }
  # Игнорировать изменения таймаутов: это предотвращает  лишние обновления ресурса при смене значений таймаутов в конфигурации
  # Но следует учитывать, что метод delete в ресурсе читает значение таймаута из стейта и, если его нужно изменить, то этот блок стоит закомментировать
  lifecycle {
    ignore_changes = [timeouts]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `magic_router_id` (String) Идентификатор Magic Router, на котором создан маршрут.
- `subnet` (String) Адрес сети назначения (CIDR).

### Optional

- `az_name` (String, Deprecated) Название зоны доступности (AZ), в которой создается маршрут. Устарело, используйте поле azName в nextHop.
- `description` (String) Описание маршрута.
- `next_hop_magic_router` (Attributes) Заполняется для создания маршрута на межпроектном соединении. (see [below for nested schema](#nestedatt--next_hop_magic_router))
- `next_hop_vpc` (Attributes) Идентификатор VPC. Заполняется при создании маршрута через соединение с VPC. (see [below for nested schema](#nestedatt--next_hop_vpc))
- `next_hop_vpc_id` (String, Deprecated) Идентификатор подключения к VPC. Устарело, используйте поле vpcConnectionId в nextHop.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `vpc_connection_id` (String, Deprecated) Идентификатор подключения к VPC. Устарело, используйте поле vpcConnectionId в nextHop.

### Read-Only

- `created_at` (String) Время создания маршрута.
- `id` (String) Идентификатор маршрута.
- `next_hop_type` (String) Тип следующего узла маршрута.
- `status` (String) Статус маршрута.
- `updated_at` (String) Время последнего изменения маршрута.

<a id="nestedatt--next_hop_magic_router"></a>
### Nested Schema for `next_hop_magic_router`

Required:

- `az_name` (String) Имя зоны доступности (AZ), в которой создан маршрут.
- `magic_router_connection_id` (String) Идентификатор соединения Magic Link.


<a id="nestedatt--next_hop_vpc"></a>
### Nested Schema for `next_hop_vpc`

Required:

- `az_name` (String) Имя зоны доступности (AZ), в которой создан маршрут.
- `vpc_connection_id` (String) Идентификатор VPC-соединения, в котором создан маршрут.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
