# cloudru_evolution_mk8s_kube_config_collection (Data Source)



## Example Usage

```terraform
data "cloudru_evolution_mk8s_kube_config_collection" "datasource_kube_config" {
  cluster_id = "00000000-0000-0000-0000-000000000000"
  # Позволяет переопределить дефолтный таймаут провайдера для определенного метода. Если нужно указать бесконечный таймаут, то нужно указать например 0s, тогда таймаута не будет.
  timeouts {
    read = "10m"
  }
}

output "data-kube_config" {
  # Конфигурация приходит в base64 формате, нужно ее декодировать.
  value = base64decode(data.cloudru_evolution_mk8s_kube_config_collection.datasource_kube_config.kube_config.config)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `cluster_id` (String) Идентификатор кластера.
- `network` (String) Тип сети, для которой запрашивается kubeconfig. Если не указано, используется сеть по умолчанию.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `kube_config` (Attributes) Kubeconfig для кластера. (see [below for nested schema](#nestedatt--kube_config))

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedatt--kube_config"></a>
### Nested Schema for `kube_config`

Read-Only:

- `config` (String) Текст kubeconfig в base64.
